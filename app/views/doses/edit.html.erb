<h2>Edit dose for <%=  @cocktail.name%></h2>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "Edit dose", class: "btn btn-primary" %>
<% end %>
